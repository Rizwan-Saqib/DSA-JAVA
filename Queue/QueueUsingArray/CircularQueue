
class CircularQueue{
 int[]arr;
 private static final int defaultsize= 5;
 int front,end;
 int cap;
 int size;
 CircularQueue(int size){
    arr = new int[size];
    front = 0 ;
    end=0;
    this.size = 0;
    }
    CircularQueue(){
    this(defaultsize);
    
    }
    
public boolean isFull(){

return arr.length ==size ;
}
public boolean  isEmpty(){

return size == 0;
}
public boolean insert(int val){
if(isFull()){
return false;
}
arr[end++]= val;
end = end%arr.length;
size++;
return true;
}
class  CustomException extends Exception{
public CustomException(String message){
super(message);
}

}
public int remove()throws CustomException{
if(isEmpty()){
throw new CustomException("the stack is emty");

}
int removed = arr[front++];
front = front%arr.length;
size--;
return removed;
}
public void Display(){
for(int i = front ; i < end;i++){
System.out.println(arr[i]);


}

}

public static void main(String args[])throws CustomException{
CircularQueue queue = new CircularQueue();
queue.insert(5);
queue.insert(10);
queue.insert(15);
queue.Display();
System.out.println(queue.remove());
System.out.println(queue.remove());
}
}
// for my own uderstainf the things that make me confysed -- > the size varaiable like we have one size variavle and we are using it at two dofferent places ad leeting to to do two dofferent operatins
//actally there are two sizes in the code one is the arr.length  we can say it is a permanent because in cicular queue we cant change the size we cant oveflow the queue when we reach the limit we simpley override the  element at first  index 
//and of we talk about the actaual size variable it is initially zero and eith theaddition and removal of elmets it was increasing and deceasing 
